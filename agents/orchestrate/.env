#GOOGLE_API_KEY=xx
GOOGLE_CLOUD_PROJECT=pokedemo-test
GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_LOCATION=us-central1

# Base URL for *this* orchestrator agent service itself
# Adjust the port (e.g., 8080) if your service runs elsewhere
AGENT_BASE_URL="http://localhost:8080"

# Addresses of the remote agents the orchestrator communicates with
# Note: Standard .env format is KEY=VALUE, not Python list syntax.
# The python code reads this as a comma-separated string.
REMOTE_AGENT_ADDRESSES="http://localhost:10001,http://localhost:10002,http://localhost:10003"
#REMOTE_AGENT_ADDRESSES="https://social-agent-service-789872749985.us-central1.run.app,https://planner-agent-service-789872749985.us-central1.run.app,https://platform-mcp-client-789872749985.us-central1.run.app"